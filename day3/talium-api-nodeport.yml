apiVersion: v1
kind: Service
metadata:
  name: talium-api-nodeport
  namespace: talium
spec:
  type: NodePort
  selector:
    app: talium-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
